apiVersion: v1
kind: Service
metadata:
  name: health-tracker-service
  namespace: default
spec:
  selector:
    app: health-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
