apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: health-tracker-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: health-tracker
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /api/metrics
      interval: 30s
